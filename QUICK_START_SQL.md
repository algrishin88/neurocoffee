# ⚡ Быстрый старт с SQL

## За 3 шага к запуску!

### Шаг 1: Установка

```bash
npm install
```

### Шаг 2: Создайте .env файл

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL="file:./dev.db"
PORT=3000
JWT_SECRET=ваша-случайная-строка-минимум-32-символа
```

### Шаг 3: Инициализация и запуск

```bash
npm run init-db
npm run dev
```

**Готово!** 🎉

Сервер работает на `http://localhost:3000`

---

## Что изменилось?

✅ **MongoDB заменен на SQLite** - не нужно устанавливать сервер БД
✅ **Prisma ORM** - современный и удобный
✅ **Файл базы данных** - `dev.db` в корне проекта
✅ **Все заказы сохраняются** в таблицу `orders`

## Просмотр данных

```bash
npm run prisma:studio
```

Откроется визуальный редактор базы данных в браузере.

## Структура БД

Все данные в SQL таблицах:
- `users` - пользователи
- `carts` - корзины
- `orders` - заказы (заполняется при оформлении)
- `bookings` - бронирования
- `contacts` - сообщения
- `menu_items` - товары меню

---

**Подробнее:** См. `README_SQL.md` и `SQL_SETUP.md`

