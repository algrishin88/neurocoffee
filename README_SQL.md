# 🚀 НейроКофейня - Backend на SQL

Backend сервер для НейроКофейни на Node.js с **SQLite** (можно легко переключиться на PostgreSQL/MySQL).

## ✨ Преимущества SQL

- ✅ **SQLite** - не требует установки сервера, работает из коробки
- ✅ **Prisma ORM** - современный и удобный
- ✅ **Типобезопасность** - автогенерация типов
- ✅ **Миграции** - версионирование схемы БД
- ✅ **Prisma Studio** - визуальный редактор данных

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Создание .env файла

Создайте файл `.env` в корне проекта:

```env
# Database (SQLite - работает из коробки!)
DATABASE_URL="file:./dev.db"

# Server
PORT=3000

# JWT Secret (измените на случайную строку!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# YandexGPT API (опционально)
YANDEX_API_KEY=your-yandex-api-key
YANDEX_FOLDER_ID=your-folder-id

# Environment
NODE_ENV=development
```

### 3. Инициализация базы данных

```bash
npm run init-db
```

Этот скрипт:
- ✅ Создаст SQLite базу данных (`dev.db`)
- ✅ Создаст все таблицы
- ✅ Заполнит меню товарами

### 4. Запуск сервера

```bash
npm run dev
```

Сервер будет доступен на: `http://localhost:3000`

## 📋 Структура базы данных

### Таблицы:

- **users** - пользователи
- **carts** - корзины пользователей
- **cart_items** - товары в корзине
- **orders** - заказы (заполняется при оформлении)
- **order_items** - товары в заказе
- **bookings** - бронирования столиков
- **contacts** - сообщения обратной связи
- **menu_items** - товары меню
- **menu_item_sizes** - размеры товаров

## 🔧 Полезные команды

```bash
# Генерация Prisma Client
npm run prisma:generate

# Создание миграции
npm run prisma:migrate

# Открыть Prisma Studio (визуальный редактор)
npm run prisma:studio

# Инициализация БД и меню
npm run init-db
```

## 📊 Просмотр данных

### Prisma Studio

```bash
npm run prisma:studio
```

Откроется браузер с интерфейсом для просмотра и редактирования данных.

### SQLite Browser

Скачайте: https://sqlitebrowser.org/
Откройте файл `dev.db` в корне проекта.

## 🔄 Переход на PostgreSQL или MySQL

### 1. Измените `prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"  // или "mysql"
  url      = env("DATABASE_URL")
}
```

### 2. Обновите `.env`:

```env
# PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/neirocafe?schema=public"

# Или MySQL
DATABASE_URL="mysql://user:password@localhost:3306/neirocafe"
```

### 3. Запустите миграции:

```bash
npm run prisma:migrate
```

## 📡 API Endpoints

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Корзина
- `GET /api/cart` - Получить корзину
- `POST /api/cart/add` - Добавить товар
- `PUT /api/cart/update/:itemId/:size` - Обновить количество
- `DELETE /api/cart/remove/:itemId/:size` - Удалить товар
- `DELETE /api/cart/clear` - Очистить корзину

### Заказы
- `POST /api/orders` - Создать заказ (сохраняется в БД)
- `GET /api/orders` - Получить все заказы пользователя
- `GET /api/orders/:id` - Получить заказ по ID

### Генерация кофе (AI)
- `POST /api/ai/generate-coffee` - Сгенерировать рецепт кофе

### Бронирование
- `POST /api/bookings` - Создать бронирование
- `GET /api/bookings/my` - Мои бронирования

### Контакты
- `POST /api/contacts` - Отправить сообщение

### Меню
- `GET /api/menu` - Получить все товары
- `GET /api/menu/:id` - Получить товар по ID

## 💾 Резервное копирование

SQLite база данных - это один файл `dev.db`. Просто скопируйте его!

```bash
# Создать копию
cp dev.db dev.db.backup

# Восстановить
cp dev.db.backup dev.db
```

## 🐛 Решение проблем

### Ошибка: "Can't reach database server"

Убедитесь, что в `.env` указано:
```env
DATABASE_URL="file:./dev.db"
```

### Ошибка: "Migration failed"

```bash
# Удалите базу и создайте заново
rm dev.db
npm run init-db
```

### Ошибка: "Schema validation error"

```bash
# Пересоздайте Prisma Client
npm run prisma:generate
```

## 📁 Структура проекта

```
neirocafe/
├── prisma/
│   ├── schema.prisma       # Схема базы данных
│   └── migrations/         # Миграции
├── lib/
│   └── prisma.js          # Prisma Client
├── routes/                 # API маршруты
├── middleware/            # Middleware
├── scripts/               # Скрипты
├── server.js              # Главный файл
├── dev.db                 # SQLite база данных (создается автоматически)
└── .env                   # Переменные окружения
```

## ✅ Готово!

Проект готов к использованию. Все заказы сохраняются в SQLite базе данных `dev.db`.

---

**Подробная инструкция:** См. `SQL_SETUP.md`

