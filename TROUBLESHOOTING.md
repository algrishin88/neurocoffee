# 🔧 Решение проблем

## Проблема: Вход в аккаунт не сохраняется в БД

### Причина
Страницы `login.html` и `register.html` использовали localStorage вместо API.

### Решение ✅
Обновлены страницы для работы с API:
- `login.html` - теперь использует `API.auth.login()`
- `register.html` - теперь использует `API.auth.register()`
- Все страницы подключены к `js/api.js`

### Проверка
1. Убедитесь, что сервер запущен: `npm run dev`
2. Откройте консоль браузера (F12)
3. Зарегистрируйтесь или войдите
4. Проверьте в Prisma Studio: `npm run prisma:studio`
5. Должны появиться записи в таблице `users`

---

## Проблема: Ошибка при оформлении заказа

### Возможные причины:

#### 1. Пользователь не авторизован
**Решение:** Убедитесь, что вы вошли в аккаунт через API (не через localStorage)

#### 2. Корзина пуста
**Решение:** Добавьте товары в корзину перед оформлением

#### 3. Сервер не запущен
**Решение:** Запустите `npm run dev`

#### 4. Неправильный формат данных
**Решение:** Проверьте консоль браузера (F12) на ошибки

### Проверка заказов в БД

```bash
npm run prisma:studio
```

Откройте таблицу `orders` - там должны быть все заказы.

---

## Как проверить, что всё работает

### 1. Проверка сервера

```bash
# Сервер должен быть запущен
npm run dev

# Должно появиться:
# ✅ Подключено к SQLite базе данных
# 🚀 Сервер запущен на порту 3000
```

### 2. Проверка API

Откройте в браузере:
- `http://localhost:3000/api/health` - должен вернуть `{"status":"OK"}`
- `http://localhost:3000/api/menu` - должен вернуть список товаров

### 3. Проверка авторизации

1. Откройте `register.html`
2. Зарегистрируйтесь
3. Откройте `login.html`
4. Войдите
5. Проверьте в Prisma Studio - должен появиться пользователь

### 4. Проверка заказов

1. Войдите в аккаунт
2. Добавьте товары в корзину
3. Оформите заказ
4. Проверьте в Prisma Studio - должен появиться заказ в таблице `orders`

---

## Отладка

### Консоль браузера (F12)

Проверьте вкладку **Console** на ошибки:
- `API Error: ...` - проблема с API
- `CORS error` - проблема с CORS
- `Network error` - сервер не запущен

### Консоль сервера

Проверьте терминал, где запущен сервер:
- Ошибки подключения к БД
- Ошибки валидации
- Ошибки API запросов

### Prisma Studio

```bash
npm run prisma:studio
```

Проверьте данные в таблицах:
- `users` - пользователи
- `orders` - заказы
- `carts` - корзины

---

## Частые ошибки

### "API не загружен"

**Решение:** Убедитесь, что `js/api.js` подключен в HTML:
```html
<script src="js/api.js"></script>
```

### "Токен не предоставлен"

**Решение:** Войдите в аккаунт заново через `login.html`

### "Пользователь не найден"

**Решение:** Зарегистрируйтесь заново через `register.html`

### "Корзина пуста"

**Решение:** Добавьте товары в корзину перед оформлением заказа

---

## Проверка данных в БД

### Через Prisma Studio (рекомендуется):

```bash
npm run prisma:studio
```

### Через SQL запросы:

Откройте `dev.db` в SQLite Browser и выполните:

```sql
-- Все пользователи
SELECT * FROM users;

-- Все заказы
SELECT * FROM orders;

-- Товары в заказе
SELECT * FROM order_items WHERE orderId = 'ваш-order-id';
```

---

## Если ничего не помогает

1. **Очистите localStorage:**
   ```javascript
   // В консоли браузера (F12)
   localStorage.clear();
   sessionStorage.clear();
   ```

2. **Перезапустите сервер:**
   ```bash
   # Остановите (Ctrl+C)
   npm run dev
   ```

3. **Пересоздайте БД:**
   ```bash
   rm dev.db
   npm run init-db
   ```

4. **Проверьте .env файл:**
   ```env
   DATABASE_URL="file:./dev.db"
   PORT=3000
   JWT_SECRET=ваш-секретный-ключ
   ```

