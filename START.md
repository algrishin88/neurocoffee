# 🚀 Запуск проекта НейроКофейня

## Быстрая установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка MongoDB

#### Вариант A: MongoDB Atlas (рекомендуется)

1. Зарегистрируйтесь на https://www.mongodb.com/cloud/atlas
2. Создайте бесплатный кластер M0
3. Создайте пользователя базы данных
4. Получите connection string
5. Добавьте ваш IP в whitelist (или 0.0.0.0/0 для тестирования)

#### Вариант B: Локальная установка

1. Установите MongoDB: https://www.mongodb.com/try/download/community
2. Запустите MongoDB сервис
3. MongoDB будет доступна на `mongodb://localhost:27017`

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# MongoDB
MONGODB_URI=mongodb://localhost:27017/neirocafe
# Или для MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/neirocafe

# Server
PORT=3000

# JWT Secret (измените на случайную строку!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# YandexGPT API (опционально, для генерации кофе)
# Получите на https://cloud.yandex.ru/
YANDEX_API_KEY=your-yandex-api-key
YANDEX_FOLDER_ID=your-folder-id

# Environment
NODE_ENV=development
```

### 4. Инициализация базы данных

```bash
npm run init-db
```

Этот скрипт:
- Подключится к MongoDB
- Создаст коллекции
- Заполнит меню товарами

### 5. Запуск сервера

#### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

#### Продакшн режим:
```bash
npm start
```

Сервер будет доступен на: `http://localhost:3000`

### 6. Проверка работы

Откройте в браузере:
- `http://localhost:3000/api/health` - должен вернуть `{"status":"OK"}`
- `http://localhost:3000/api/menu` - должен вернуть список товаров

## 🔧 Настройка YandexGPT (опционально)

Для работы функции "Сгенерировать ваш нейро-кофе":

1. Зарегистрируйтесь на https://cloud.yandex.ru/
2. Создайте каталог (folder)
3. Получите API ключ
4. Добавьте в `.env`:
   ```env
   YANDEX_API_KEY=ваш-api-ключ
   YANDEX_FOLDER_ID=ваш-folder-id
   ```

**Примечание:** Если YandexGPT не настроен, функция генерации будет использовать fallback рецепт.

## 📋 Структура API

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Корзина
- `GET /api/cart` - Получить корзину
- `POST /api/cart/add` - Добавить товар
- `PUT /api/cart/update/:itemId/:size` - Обновить количество
- `DELETE /api/cart/remove/:itemId/:size` - Удалить товар
- `DELETE /api/cart/clear` - Очистить корзину

### Заказы
- `POST /api/orders` - Создать заказ (сохраняется в БД)
- `GET /api/orders` - Получить все заказы пользователя
- `GET /api/orders/:id` - Получить заказ по ID

### Генерация кофе (AI)
- `POST /api/ai/generate-coffee` - Сгенерировать рецепт кофе

### Бронирование
- `POST /api/bookings` - Создать бронирование
- `GET /api/bookings/my` - Мои бронирования

### Контакты
- `POST /api/contacts` - Отправить сообщение

### Меню
- `GET /api/menu` - Получить все товары
- `GET /api/menu/:id` - Получить товар по ID

## 🗄️ База данных

Все данные сохраняются в MongoDB:

- **Users** - пользователи
- **Carts** - корзины пользователей
- **Orders** - заказы (сохраняются при оформлении)
- **Bookings** - бронирования столиков
- **Contacts** - сообщения обратной связи
- **MenuItems** - товары меню

## 🎯 Функционал

### ✅ Реализовано:

1. **Авторизация и регистрация** - JWT токены, хеширование паролей
2. **Корзина** - синхронизация с БД
3. **Заказы** - сохранение в БД при оформлении
4. **Генерация кофе** - через YandexGPT API
5. **Бронирование столиков** - сохранение в БД
6. **Обратная связь** - сохранение сообщений в БД

### 📝 Заказы в базе данных

При оформлении заказа через `POST /api/orders`:
- Заказ сохраняется в коллекцию `orders`
- Корзина автоматически очищается
- Заказ содержит все товары, общую сумму, статус
- Можно получить все заказы пользователя через `GET /api/orders`

## 🐛 Решение проблем

### MongoDB не подключается
- Проверьте, что MongoDB запущен
- Проверьте правильность MONGODB_URI в `.env`
- Для Atlas: проверьте whitelist IP адресов

### Порт 3000 занят
Измените `PORT` в `.env` на другой порт

### YandexGPT не работает
- Проверьте правильность API ключа и Folder ID
- Функция будет использовать fallback рецепт

### CORS ошибки
Убедитесь, что сервер запущен и доступен

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи сервера в консоли
2. Консоль браузера (F12)
3. Файл `.env` на правильность настроек

---

**Готово к запуску!** 🎉

