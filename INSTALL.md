# 📦 Полная инструкция по установке и запуску

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка MongoDB

### Вариант A: MongoDB Atlas (рекомендуется для начала)

1. Перейдите на https://www.mongodb.com/cloud/atlas
2. Создайте бесплатный аккаунт
3. Создайте новый кластер (бесплатный M0)
4. Нажмите "Connect" → "Connect your application"
5. Скопируйте connection string
6. Создайте пользователя базы данных (Database Access)
7. Добавьте IP адрес в Network Access (или 0.0.0.0/0 для тестирования)

### Вариант B: Локальная установка

1. Скачайте MongoDB: https://www.mongodb.com/try/download/community
2. Установите и запустите MongoDB
3. MongoDB будет доступна на `mongodb://localhost:27017`

## Шаг 3: Создание .env файла

Создайте файл `.env` в корне проекта:

```env
# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/neirocafe
# Или для MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/neirocafe

# Server Port
PORT=3000

# JWT Secret (ОБЯЗАТЕЛЬНО измените на случайную строку!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345

# YandexGPT API (опционально, для генерации кофе)
# Получите на https://cloud.yandex.ru/
YANDEX_API_KEY=your-yandex-api-key
YANDEX_FOLDER_ID=your-folder-id

# Environment
NODE_ENV=development
```

## Шаг 4: Инициализация базы данных

```bash
npm run init-db
```

Этот скрипт:
- ✅ Подключится к MongoDB
- ✅ Создаст все необходимые коллекции
- ✅ Заполнит меню товарами

## Шаг 5: Запуск сервера

### Режим разработки (рекомендуется):
```bash
npm run dev
```

### Продакшн режим:
```bash
npm start
```

Сервер будет доступен на: `http://localhost:3000`

## Шаг 6: Проверка работы

Откройте в браузере:
- ✅ `http://localhost:3000/api/health` - должен вернуть `{"status":"OK"}`
- ✅ `http://localhost:3000/api/menu` - должен вернуть список товаров

## Шаг 7: Подключение API к фронтенду

Убедитесь, что в ваших HTML файлах подключен API:

```html
<!-- В menubeta.html, index.html, contact.html, services.html -->
<script src="js/api.js"></script>
<script src="js/cart.js"></script>
```

## 🎯 Функционал "Ваш нейро-кофе"

### Настройка YandexGPT (опционально):

1. Зарегистрируйтесь на https://cloud.yandex.ru/
2. Создайте каталог (folder)
3. Получите API ключ в разделе "Сервисные аккаунты"
4. Добавьте в `.env`:
   ```env
   YANDEX_API_KEY=ваш-api-ключ
   YANDEX_FOLDER_ID=ваш-folder-id
   ```

**Примечание:** Если YandexGPT не настроен, функция будет использовать fallback рецепт.

### Использование:

1. Войдите в аккаунт
2. Перейдите в меню
3. Найдите "Ваш нейро-кофе"
4. Нажмите "Сгенерировать"
5. Опишите ваше настроение
6. Получите уникальный рецепт
7. Добавьте в корзину

## 📊 Заказы в базе данных

При оформлении заказа:
- ✅ Заказ автоматически сохраняется в коллекцию `orders`
- ✅ Содержит все товары, общую сумму, статус
- ✅ Корзина автоматически очищается
- ✅ Можно получить все заказы через API: `GET /api/orders`

### Просмотр заказов в MongoDB:

```javascript
// В MongoDB Compass или через mongo shell
db.orders.find().pretty()
```

## 🗂️ Структура базы данных

После инициализации создаются коллекции:

- **users** - пользователи
- **carts** - корзины
- **orders** - заказы (заполняется при оформлении)
- **bookings** - бронирования
- **contacts** - сообщения
- **menuitems** - товары меню

## 🚨 Решение проблем

### Ошибка подключения к MongoDB

```
❌ Ошибка подключения к MongoDB
```

**Решение:**
- Проверьте, что MongoDB запущен
- Проверьте правильность MONGODB_URI в `.env`
- Для Atlas: проверьте whitelist IP адресов

### Порт 3000 занят

**Решение:**
Измените `PORT` в `.env` на другой порт (например, 3001)

### YandexGPT не работает

**Решение:**
- Проверьте правильность API ключа и Folder ID
- Функция будет использовать fallback рецепт
- Проверьте баланс в Yandex Cloud

### CORS ошибки

**Решение:**
- Убедитесь, что сервер запущен
- Проверьте адрес API в `js/api.js` (по умолчанию `http://localhost:3000/api`)

## ✅ Чеклист готовности

- [ ] Установлены зависимости (`npm install`)
- [ ] Создан файл `.env` с правильными настройками
- [ ] MongoDB настроен и доступен
- [ ] База данных инициализирована (`npm run init-db`)
- [ ] Сервер запущен (`npm run dev`)
- [ ] API доступен (`http://localhost:3000/api/health`)
- [ ] API подключен к фронтенду (`js/api.js` в HTML)
- [ ] (Опционально) YandexGPT настроен для генерации кофе

## 🎉 Готово!

Проект готов к использованию. Все заказы будут сохраняться в базе данных MongoDB.

---

**Вопросы?** Проверьте логи сервера и консоль браузера (F12)

